# 2026-07-31 2인 분담 검토 — 거래량 알림 개편 (461ce22f · 71393fe0)

배포·검증(CI/첫 회차) 완료 후 사장님 지시로 개발자 A·B 범위 분담 검토.
양쪽 모두 레포 읽기 전용·DB 는 sqlite3.backup 사본만 사용. **공통 결론: major 0건, 배포 차단 사유 없음.**

## 개발자 A — 신규 로직 내부 정확성

검증 수단: 커밋 정독 + 업비트 실API 손계산 교차검증(오차 0.000%) + 프로덕션 DB
실측 + 테스트 245/245.

**검증 통과**: fetch_rvol_1h 수식·경계(진행봉/직전완결봉/21h창 제외 실물 확인,
분자·분모 겹침 0) / 밴드 등록 원자성·터치환율 고정·long 전용 / tps 오름차순
저장 계보 추적(extractor 단일 생산자 → `_tps[-1]`=최고 TP 참) / 다음 TP 선정
경계 동치 / 예고 스위치 분리·복원 / DB 실측(JUP·CFX 밴드 손 재계산 일치, BTC
+10% 폴백 실데이터 확인).

**발견**:
- [minor] **A-1 터치 알림(tp_usd)과 급증 알림 다음 TP(tps_usd)의 표기 갈림** —
  실데이터 ETH id=169 가 tp_usd=2000 / tps_usd=[1975] 불일치. 원인은 extractor
  데이터 계보(오늘 코드 아님)이나 새 표시 기능이 처음 노출. 표시 일원화 제안.
- [info ×6] cur==0.0 truthy 가드 / 분자 분경계 스냅·count 절단(보수 방향) /
  분모 제외로 생기는 최대 59분 사각(자기희석 제거의 의도된 대가) / bool 통과 /
  정렬 재보증 없음·tps_krw 파손 시 폴백 미작동(자가 JSON 이라 실발생 없음) /
  현행 감시 9행 전부 레거시(밴드 NULL, 새 기능은 다음 터치부터).

## 개발자 B — 경계면·회귀 + S9 통합 감사

**검증 통과**: 마이그레이션 멱등(사본 init_db 2회 불변, 2연속 스키마 변경 순서
문제 없음, origin 최신 DB 10컬럼 확인) / add_volume_watch 3분기 SQL 대조 /
예고 OFF 경계면(카운터·상태머신·적중판정 무영향) / 회차 시간 실측(개편 전
49~62s vs 후 49~71s, 전건 success — 유의미 증가 없음) / 테스트 약화 없음
(커밋1 순수 추가, 커밋2 삭제 2건은 강화 대체).

**발견**:
- [minor] **B-1 만료행 재터치 race** — alerted=0 인데 72h 를 갓 넘긴(prune 전)
  행에 재터치가 오면 활성 분기(타이머 비갱신)로 빠지고, 같은 회차 말미 prune 이
  방금 유효해진 감시를 삭제(무성 소실 잔존 변종, 경합창 ~1회차). 수정:
  add_volume_watch 에 창을 전달해 만료행은 전면 리셋, 또는 prune 을 초입으로.
- [minor] **B-2 감시 시세의 markets 결합** — 밴드 행 시세가 ret_pending(~78h)
  포함에 암묵 의존. 소급 터치 6h+ 지연 시 감시 말미 유예 표류 가능. 수정:
  markets 에 감시 티커 합집합(1줄).
- [minor] **B-3 분자 count=60 경계 절단** — 61캔들 케이스 최고령 1분 누락
  (보수 방향). 수정: count=61 또는 주석 정정.
- [info ×5] 레거시 행 재터치에도 NULL 유지(08-02 자연 소멸) / show_status
  전환율 분자 tp* 혼입(기존 사항, 체감 증가) / SELECT→분기 비원자(단일 라이터라
  무해) / 급증 알림 원장 미기록(커밋백 실패 시 중복 1회 — 07-29 승계, 기지
  한계와 동류) / 밴드 이탈 스팟 1샘플 one-way(의도 확정 사항).

**S9 통합 감사 (TP 클러스터 중복 차단 d569e8d)**:
- 사고 원본(AERO 110/111 tp1 동시각 2건)은 수정 배포 **이전** 건 — 재발 아님.
- 배포 후 TP 알림 3건(FIL·SEI) 전부 단독 레벨 — 중복 0건.
- 차단 로직 **실발동은 아직 0건**: 멀티TP+양쪽 실터치 형제쌍은 AERO 110/111 이
  유일, 둘 다 tp_alert_idx=1 동기 상태. **AERO 가 TP2(0.45) 도달 시 첫 실사례**
  — 그때 원장 차단 로그 확인이 다음 액션.
- 07-31 BTC 클러스터(162+168): 원장 단일 엔트리, 162 실터치/168 섀도 — 정상.
- 한계 3건(설계 범위): 같은 서수만 비교 / 창 10분 / 직전 종결 형제 미차단.

## 조치 — 4건 전부 적용 완료 (사장님 "전체 수정 후 푸시" 지시, 당일)

| # | 항목 | 적용 내용 |
|---|---|---|
| B-1 | 만료행 재터치 race | add_volume_watch 에 max_age_sec — 창 넘긴 미발송 행은 전면 리셋 (VS14/VS14b) |
| B-2 | 감시 시세 결합 | run_once markets 에 감시 티커 합집합 |
| B-3 | 분자 count 경계 | count 60→61 + 주석 정정 |
| A-1 | TP 표기 갈림 | _volume_band_tps 가 tp_usd 를 합집합·정렬 재보증 — extractor 무수정으로 표시 일원화 (PV8f/PV8g) |

관찰 항목: AERO TP2 도달 시 S9 차단 첫 실사례 확인 / 첫 신규 터치 후 저장
밴드 실물 재검(A-⑦).
