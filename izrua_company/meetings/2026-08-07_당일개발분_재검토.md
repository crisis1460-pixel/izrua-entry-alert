# 2026-08-07 당일 개발분 재검토 (커밋 bef75a50 + 907f58a7)

리뷰 체제: 에이전트 2종 병렬 — A(정합성 추적: 수정이 진짜 고쳐졌나/새 버그 유입),
B(커버리지·일관성: 회귀 테스트 공백/같은 클래스 미수정 잔존/문서 정합).
반박 생존 발견만 채택. 결과 커밋: `759c06f4`.

## 발견 → 수정 3건

**R1 [critical] C1(whitelist COALESCE) 수정이 무효였다**
- SQL 은 `COALESCE(?, author_whitelisted)` 로 바뀌었지만 바인딩이
  `1 if ... else 0` 그대로 → NULL 이 SQL 에 도달할 수 없음 = 죽은 코드.
- 상류 run_collect:281 도 `stats_row.get("whitelisted", False)` — None 불가.
- 시나리오: 화이트 작성자(DB=1) → 재수집 시 워쳐 통계 로드 실패(토큰/네트워크)
  → False → 0 바인딩 → COALESCE(0,1)=0 → ⭐⭐ 소실. **원버그 그대로 잔존.**
- 수정(3상 의미론): 통계 미로드 → None(알 수 없음, 기존 보존) /
  로드됨+화이트 → 1 확정 / 로드됨+비화이트 → 0 확정(디화이트 전파 유지).

**R2 [warning] W5 같은 클래스 잔존 6곳**
- `now or time.time()` 이 db.py 5곳(rets_needing_record, mark_deletion_checked,
  record_alert, prune_daily_stats, prune_closed_levels) + price_check.run_once
  본체에 남아 있었다. 전부 `is not None` 패턴으로 일소.

**R3 [info] extractor tp_ladder_count 끝값 불일치**
- tps_all 은 v[-1](오늘 W4 수정), 단계 수는 v[0] 그대로 → 범위형 스펙
  (TP1: 5.0~5.5)에서 하한은 방향필터 탈락·상한은 생존 → "1/N단계" 표시와
  감시 목록 개수가 어긋남. v[-1] 로 통일.

## 이상 없음 확인 (반박 생존 실패 = 리포트 제외된 의심들)
- price_check entry_usd 가드: clustering 이 이미 falsy 걸러 사실상 사문(무해).
- record_ret commit: 이벤트별 선커밋(908행) 있어 부분커밋 위험 없음.
- run_cycle META 분리: 4개 부분실패 조합 전수 — 중복발송 경로 없음.
- OKX: 뒤집기 시간순 정합, 표본 99≥91 안전, 부분응답은 fail-safe(None).
- shadow_touch 소비자: 읽는 곳 db.py 1곳뿐 — 일관.

## 잔여 리스크 (수정 안 함, 기록만)
- shadow_touch 마커는 소급 불가 — 08-07 이전 만료된 섀도 행은 여전히
  untouched_expired 로 집계(터치율 분모 오염). 시간이 지나면 희석.
- OKX 8h 간격 가정 — 가변 간격 상품이면 neg_days 표시 부정확(배지 표시 전용,
  Bybit 때부터 있던 기존 가정).
- 어제 수정 10건 중 대다수가 여전히 전용 회귀 테스트 없음(오늘 3건만 신설:
  UPS3/EX4b/G10c). 나머지는 기존 스위트가 구코드로도 통과하던 것들.

## 테스트
8종 전체 통과 (extractor 70 / grading 58(+1) / price_logic +2 / ranking 63 /
weekly_report 75 / cycle / show_status / resilience 201), check_secrets 통과.
