# 2026-08-01 재검토 — 07-31 배포 4커밋 (2인 분담)

사장님 지시("오늘 개발한 부분 위주 재검토"). 중점 = 독립 검증 없이 배포된
4241089b(2인검토 수정)·7592048a(S9 조치) + 4커밋 누적 통합 정합.
개발자 A: 뮤테이션 11종 주입 + 프로덕션 DB 실측 + 실API 교차. 개발자 B: 누적
diff 전수 + 불변식 + 프로덕션·Actions 실측. 양쪽 레포 무수정.

## 결론: major 0건 — 어제 배포분 전체 건전

- 핵심 로직은 뮤테이션 공격에도 깨지지 않았고, 신설 테스트(SA/VS14/PV8f-g)가
  주요 뮤테이션을 실제로 검출함을 확인.
- 4커밋 간 덮어쓰기·역행 없음. 원장 이중 기록·재발송 차단·상태머신·daily_stats
  의미·W1·본알림 양식 §4 전부 불변식 유지.
- 프로덕션 실측: 배포 후 24회차 전건 success·실행시간 동등(49~71s), 원장↔DB
  완전 일치, 마이그레이션 3회 반복 멱등, ETH 169 union 밴드 상단 2000 산출 확인.

## 공통 발견 (A·B 독립 도출 — 신뢰도 높음)

**R-1 [minor] TP 게이트가 NDJSON 원장을 안 봄** — touch_alert_sent 가 DB
alerts_log 만 조회. 커밋백 경합으로 DB 행이 유실되면(2026-07-28 id 68~70 실측
유형, 원장 도입 사유) 본알림 받은 신호의 TP 알림이 전부 무음. 현시점 원장 46건
전부 DB 존재(실위험 0) — 구조 리스크. 수정: 게이트에 원장 폴백 OR (1줄).

**R-2 [minor] 승계 형제(초단타)의 TP 알림이 게이트 통과** — 승계 발송 시
record_alert ids 에 B안 미달 rep 도 포함 → 그 레벨이 다중 TP(<2% 간격)면
터치 직후 tp1/tp2 고음량 연발 가능. 수정: TP 발송 분기에 `_b_swing_pass(lv)`
추가 게이트(판정·전진은 무관) — "스윙 알림 불필요" 정책의 레벨 단위 일관화.

## 개별 발견

- **A [minor] 테스트 공백 4건**: M-1 전멤버 재채점(되돌려도 255 전부 통과 —
  SA2 픽스처가 수집등급으로도 통과해서), count 61(RV 팩토리가 params 무시),
  B-2 markets 합집합, m-6 공지→감시 제거 — 전부 뮤테이션 미검출. 보강 케이스 권고.
- **A [info] count 61(B-3)은 사실상 도달 불가 케이스의 이론적 수선**(now 소수초
  정확히 0.0 필요) — 무해 유지, 근거 서술만 정정.
- **B [minor] fetch_prices 1콜 구조**: 감시 티커가 무효화(상폐)되면 배치 404 로
  회차 스킵 반경 확대 — 기존 유형(unresolved 도 동일)이며 m-6 이 완화. 백로그:
  404 시 분할 재시도.
- **B [info] M-2 소급 적용 UX**: 미종결 터치 55건 중 19건 게이트 차단 —
  FIL 152 등은 tp1·tp2 알림을 이미 받았는데 최종 TP 알림은 조용히 생략됨.
  정책의 의도된 결과지만 "따라가던 사다리 끊김" — 사용자 인지 필요(본 보고로 갈음).
- **A/B [info] `_b_swing_pass` long 전제**(short 분기 소실 — 현재 long 전용이라
  실영향 0, 숏 도입 시 복원) / cur==0.0 truthy 가드 / M-2 차단 관찰 카운터 부재
  (TP 알림 감소가 daily_stats 에 흔적 없음) / 승계 게이트 통과(A info-2 = R-2).

## 후속 권고 (미적용)

| 우선 | 항목 | 규모 |
|---|---|---|
| 1 | R-1 게이트 원장 폴백 | 1~3줄 |
| 2 | R-2 TP 발송에 _b_swing_pass 게이트 | 2줄 + 테스트 |
| 3 | A 테스트 보강 4건 (M-1 역전 픽스처 / count assert / B-2 / m-6) | 테스트만 |
| 4 | M-2 차단 관찰 카운터(로그→집계) | 소 |

기잔존: m-5 / m-7(김프 NULL — binance 451 확인 필요) / m-8 / AERO TP2 관찰.
