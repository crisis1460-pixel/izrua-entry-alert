# 2026-07-31 S9 통합 감사 — 개발자 B (미착수 건 완료)

범위: S9 = 2026-07-29~30 코드 커밋 19건 (B안 필터, R:R 제거, Partial TP 다단계
알림, Feature 4, 심리 스냅샷, 5%→2% + `_rep` 재채점, `_tp_cluster_dup` 등)
+ 07-31 개편 4커밋과의 결합. 레포 무수정·DB backup 사본·테스트 8종 통과 확인.

## Major 2건

**M-1. 알림 렌더러의 대표 선정이 `_rep` 재채점 대표와 분리**
- telegram.py:252 는 수집 score 로 자체 재선정, run_once 는 재채점 대표(rep)만
  in-place 갱신 → rep 재채점 점수 < 형제의 수집(stale) 점수면 알림에 **다른
  레벨**(작성자·등급·목표)이 표시되고 등급도 재채점값이 아님(§4 원칙 위반).
- 모듈 재현 완료(소급 터치 + 반등 회차). 07-31 BTC 는 우연히 일치한 사례.
- 수정: 클러스터 전 멤버 grade/score 재채점 갱신(1~3줄) + "렌더 대표 == 필터
  대표" 불변식 테스트.

**M-2. 본알림 필터(등급·B안)가 TP 단계 알림에 미적용 — 프로덕션 실발생**
- S9 후 TP 알림 14건 중 **10건이 본알림이 안 나간 신호**. BTC 157: 예고·터치
  둘 다 B안 억제(last TP 1.48%) → 직후 TP1~TP5 **고음량 5건** 발송. FIL 152 /
  OP 156 / AVAX 159: D등급 억제 → tp 알림 5건.
- 억제 취지와 정면 모순 + 감시 등록(발송 성공 시만)과도 비대칭.
- **질문카드 사안**: 억제 신호의 TP 알림 허용 여부는 사용자 결정.

## Minor 6건

- m-3 B안 last-TP 계산이 raw `tps_usd[-1]` — `_volume_band_tps`(union·sanity·
  정렬)와 계보 불일치. 실데이터 ETH 169/BTC 167 근거. → B안도 헬퍼 재사용.
- m-4 rep 억제 시 클러스터 전체 영구 무알림(형제 승계 없음) — 드묾, 카드 사안.
- m-5 `_tp_cluster_dup` 밴드(자기 entry ±1%)가 클러스터 정의(top −1%)와 비대칭
  — 경계 형제 미차단 가능.
- m-6 유의종목 공지 만료가 volume_watch 미정리 — 상폐성 펌핑에 급증 알림 가능.
  → 만료 시 감시 제거 1줄.
- m-7 `touch_kimchi_pct` 프로덕션 전 행 NULL — binance.com 이 Actions 미국 IP
  에서 451 차단 추정(알림 김프 행도 상시 누락 중일 것). 로그 확인 후 소스 교체 검토.
- m-8 심리 스냅샷이 send_ok 시에만 기록 — S10 분석 대상인 억제 D 군집이 전부
  결측. 터치 확정 시 기록하는 절충 제안.

## Info 5건

i-9 B안 주석 "5%" 잔존 2곳 / i-10 임계 2.0 하드코딩(TP_DISTANCE_BANDS 경계와
동시 변경 필요) / i-11 B안 비수치 방어 없음 / i-12 재터치 시 밴드는 합집합인데
tps_krw 는 첫 셋업 고정(표시 전용 어긋남) / i-13 동점 대표 결정성 id 순 의존.

## 프로덕션 실측 대사 (전부 정합)

07-31 BTC 재채점 대표 수기 재현 ✓ / B안 2% 경계(통과 3.2%·4.5%, 억제 1.48%) ✓ /
suppressed_tp_too_close 2건 = BTC 157 특정 ✓ / daily_stats 산수 3일 ✓ / 원장↔DB
46줄 ✓ / `_tp_cluster_dup` 배포 후 중복 0(실발동 0 — AERO TP2 가 첫 사례 예정) ✓ /
BTC 157 tp1→tp5→hit 상태머신 ✓.

## 테스트 공백

`_rep` 다멤버 역전 픽스처 없음(M-1 미검출 원인) / 렌더=필터 대표 불변식 없음 /
B안 tp_usd 폴백 미검증 / T35 최종 TP·상이 사다리 미검증 / TP 알림 필터 정책
테스트 부재.

## 조치 (당일 카드 확정 → 전부 적용, 커밋 참조)

- **M-2 게이트 확정**: "본알림 발송된 신호만 TP 알림" — `db.touch_alert_sent`
  게이트, 중간·최종 TP 발송 양쪽. 판정·인덱스 전진·종결은 게이트 무관 수행
  (통계 무손상). 테스트 SA1(무발송→무알림·전진)·SA4(발송→정상), T35 픽스처에
  터치 기록 보강.
- **m-4 승계 확정**: 대표가 B안 미달이면 등급·B안 통과 형제 중 재채점 최고점으로
  대표 승계 발송. 전원 미달 시만 억제·카운터 (SA2·SA3).
- **M-1**: 클러스터 전 멤버 재채점 + `render_alert(rep=)` 명시 전달 — 표시 대표
  == 필터·밴드 대표 불변식 (SA2b·SA2c).
- **m-3**: B안 TP 목록을 `_b_swing_pass` → `_volume_band_tps` 단일 출처로 통일.
- **m-6**: 유의종목 공지 만료 시 `remove_volume_watch` 즉시 호출.
- **i-9**: B안 주석 5%→2% 정정 2곳.

## 남은 항목

1. 확인 후 조치: m-7 binance 451 로그 확인 → 김프 소스 교체 검토
2. 미조치 minor: m-5 `_tp_cluster_dup` 밴드 비대칭(경계 형제), m-8 억제 터치의
   심리 스냅샷 결측(S10 분석 편향) — 다음 정비 후보
3. 관찰: AERO TP2 시 `_tp_cluster_dup` 첫 실발동, 신규 터치 밴드 실물 재검
