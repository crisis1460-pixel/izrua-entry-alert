# 2026-08-07 밤 전체 코드 감사 + 유지보수 (23:19~23:40)

사용자 지시: "능동적으로 코드 오류 찾아 수정, 유지보수 컨셉으로 계속 진행,
확인받지 말고, 결과만 보고." 아래는 그 세션의 전체 기록.

## 1단계 — 7개 에이전트 병렬 감사

**오늘 개발분(11커밋) 정밀검토 2건**: 정합성 추적 / 커버리지·사용자지시(내부
전용 원칙) 준수 검증.
**전체 코드베이스 모듈별 감사 4건**: collector+analytics / monitor /
storage/db.py / notify+scripts+config — 파일 전체를 처음부터 끝까지 정독.
**GitHub Actions 워크플로 감사 1건**: price-check/tests/secret-scan/
weekly-report.yml 4개 파일.

### 확정 수정 6건 (커밋 25ac4d35, 3ca798da)
1. **[critical]** `price_check.py:724-747` 자체성적 주입 블록 예외 미격리.
   같은 파일 다른 블록은 전부 try/except 인데 이 블록만 무보호 —
   send_ok=True 시점 예외가 connect() with 블록을 그대로 빠져나가 그 회차
   다른 티커의 터치·알림·daily_stats 까지 통째로 롤백되는 최악의 실패 모드.
2. **[warning]** `db.py record_touch_verdicts` — (label,reason) 2-tuple
   계약 위반 입력(1원소) 시 IndexError로 같은 롤백 위험. len(v)<2 방어.
3. **[warning]** `oi_history` 프룬 쿼리가 복합 인덱스를 못 써 매 적재마다
   전체 스캔 — ts 단독 인덱스 추가. **EXPLAIN QUERY PLAN 으로 실제
   인덱스 사용 전환 확인 완료.**
4. **[info]** `alert_ledger.py:142` — `now or time.time()` 낙수 케이스
   하나 잔존(db.py 는 전량 수리됐으나 이 파일만 누락).
5. **[warning]** `check_secrets.py` GENERIC_ASSIGN — 따옴표 감싼 값만
   탐지, YAML 스타일(`token: abc...`) 유출 놓침. 확장 시도한 bare "key"
   변수명은 SQL `key='...'` 오탐 확인 후 보류(실측 후 되돌림).
6. **[info]** `HANDOFF.md` "알림 양식 최종 확정" 섹션이 08-07/08-08 신규
   라인(🧭 수급·🌡️ 자리) 미반영 — 갱신. 테스트 개수 표기 7종→8종 오기 정정.

### 커버리지 공백 4건 신설 (교차감사가 무테스트 확인한 것들)
OI1~OI3(get_oi_baseline 18h/30h 창 경계) / OI4(48h 프룬) /
OI5a,b(60분 게이트 + CoinGecko 실패 시 게이트 미갱신) / CG1,2(TTL 캐시
히트·만료) / MA200-1(record_touch_verdicts ma200_above, 기존 VR1/VR2 는
이 인자 없이 호출해 완전 무검증).

### 워크플로 감사 결과: 전부 clean
single-writer discipline, timeout 여유, secrets 매칭, data 커밋백 로직,
동시성 격리, YAML hygiene — 오늘 신규 기능(OI 스냅샷·MA·CoinGecko)이
워크플로 변경을 요구하지 않는다는 가정을 실제로 검증, 이상 없음.

## 2단계 — 실전 데이터 종단간(E2E) 드라이런

프로덕션 DB(origin/main, 279 레벨/230 활성) 를 스크래치 사본으로 받아
telegram.send 만 목킹하고 **실제 시장 데이터로** 전 파이프라인 실행.

- `price_check.run_once()` 정상 완료(11.5s, 예외 0). 이번 회차 우연히
  터치 0건이라, ETH long 레벨 1건을 강제 터치 시나리오로 재구성해 재실행
  → 🎯 터치 알림 정상 발송, `🌡️ 자리`/`🧭 수급` 줄 실제 계산값으로 렌더링
  확인, DB 기록(touch_supply_verdict/position/ma200_above) 표시값과 일치.
- `run_weekly_report.send_report()` 정상 완료(작성자 30명·종결 126건 실
  데이터) — E_LB 랭킹/2트랙/초과적중률/캘리브레이션(신구 산식 병기)/
  R-멀티플/보유기간/역신호 후보·확정 전 섹션 렌더 정상, 예외 0.
  **판정 데이터(🧭/🌡️) 어디에도 미노출 확인** — 정적 grep 뿐 아니라
  실제 렌더 출력으로도 재확인.
- 실제 코인 10종(BTC/ETH/SOL/XRP/DOT/INJ/DOGE/ADA/LINK/ATOM) 펀딩
  히스토리 라이브 조회 — 전부 99개 정상 수신, 플립 오탐 0건.

## 3단계 — 플래그 항목 데이터로 확정 (오늘 오전 감사에서 미해결로 남겼던 것)

collector/analytics 감사가 "extractor.py tp vs tps_all[0] 불일치 가능성
(warning)"을 재발견 — 오전 재검토 때는 관찰 확정 없이 보류만 했던 항목.

실측: 프로덕션 유효표본 120건 중 불일치 2건(id=169, id=254). id=169는
2026-07-30 수집(grade_ver=None, 완전 레거시). **id=254 는 2026-08-06
수집(v4) — 원문 추적 결과 "Target (TP): 2.185 – 2.210"(콜론 라벨+범위값
단일그룹) 패턴으로 확인.** 이는 정확히 오늘 아침 bef75a50 커밋의 W4
수정(`tps_all is_spec` 분기 `v[0]→v[-1]`)이 겨냥한 패턴 — 수정 시각
(08-07 07:34)이 id=254 수집시각(08-06 05:38)보다 늦어 **이미 해결된
버그의 잔존 구데이터**로 확정. 현재 코드는 정상, 추가 조치 불필요.
두 표본 모두 status=touched(비활성)라 reparse_all 대상도 아님.

실전 사례를 그대로 회귀 테스트(ICP 케이스)로 고정 — 커밋 ff41a65f.

## 결론

오늘 개발분(펀딩 소스 전환·수급 판정·RSI/MA 자리 판정·판정 자가검증·
MA 확장) 전체가 정적 감사 7건 + 실전 E2E 드라이런 2건을 모두 통과.
확정 수정 6건 + 실전 검증 회귀테스트 15건 신설, 테스트 8종 전체 통과,
check_secrets 통과. 프로덕션 워크플로 최근 실행 전부 정상(격리된 1건
"cancelled" 는 기존에 분석된 미국 피크시간 인프라 취소 패턴과 일치,
데이터 커밋 연속성 무손상).

미해결 항목 없음.
